# План создания агента Anthropic: Kotlin/Ktor + Modern Frontend

## 1. Архитектура решения

### Компоненты системы:
- **Backend** (Kotlin/Ktor) — HTTP сервер, логика агента, Tool Use
- **Frontend** (HTML/CSS/JS) — современный интерфейс чата (Tailwind CSS + Vanilla JS)
- **API интеграция** — клиент Anthropic API
- **Tool Use система** — агент вызывает инструменты при необходимости

---

## 2. Стек технологий

### Backend:
- **Kotlin** — язык программирования (типизированный, безопасный)
- **Ktor** — легкий веб-фреймворк для Kotlin
- **Kotlinx Serialization** — JSON сериализация
- **HTTP Client** — встроенный HttpClient для запросов к Anthropic API
- **Dotenv** — загрузка переменных окружения

### Frontend:
- **HTML5** — структура
- **Tailwind CSS** — современные стили без написания CSS
- **Vanilla JavaScript** — простой, без фреймворков
- **Fetch API** — HTTP запросы

### Почему этот стек?
- ✅ Kotlin типизирован → меньше ошибок в runtime
- ✅ Ktor минималистичен → быстро и просто
- ✅ Tailwind позволяет создавать красивый UI за минуты
- ✅ Vanilla JS достаточно для чата → не нужен React/Vue

---

## 3. Поток работы агента (Tool Use цикл)

```
1. Пользователь пишет сообщение в чат → отправляет GET/POST запрос на /chat
   ↓
2. Backend (Kotlin) получает запрос
   ↓
3. Backend отправляет в Claude API:
   - История сообщений (messages)
   - Список доступных инструментов (tools)
   ↓
4. Claude анализирует и решает:
   - Нужен ли инструмент?
   - Какой?
   ↓
5a. Если инструмент нужен (tool_use):
   - Backend вызывает соответствующую функцию
   - Результат отправляется обратно в Claude
   - Claude формирует финальный ответ
   ↓
5b. Если инструмент не нужен:
   - Claude сразу возвращает ответ
   ↓
6. Backend возвращает JSON ответ фронтенду
   ↓
7. Frontend обновляет чат в реальном времени
```

---

## 4. Инструменты (Tools)

### Инструмент 1: Оценка физической подготовки
```json
{
  "name": "assess_fitness_level",
  "description": "Оценивает уровень физической подготовки на основе данных пользователя и возвращает рекомендацию",
  "input_schema": {
    "type": "object",
    "properties": {
      "age": {"type": "integer", "description": "Возраст пользователя"},
      "running_experience": {
        "type": "string",
        "enum": ["beginner", "intermediate", "advanced"],
        "description": "Опыт бега (новичок, средний уровень, продвинутый)"
      },
      "weekly_runs": {"type": "integer", "description": "Количество пробежек в неделю"},
      "max_distance_km": {"type": "number", "description": "Максимальная дистанция, которую может пробежать (км)"}
    },
    "required": ["age", "running_experience", "weekly_runs", "max_distance_km"]
  }
}
```

### Инструмент 2: Генерация тренировочного плана
```json
{
  "name": "generate_training_plan",
  "description": "Создаёт персональный 4-недельный тренировочный план по бегу на основе уровня подготовки",
  "input_schema": {
    "type": "object",
    "properties": {
      "fitness_level": {
        "type": "string",
        "enum": ["beginner", "intermediate", "advanced"],
        "description": "Уровень физической подготовки"
      },
      "goal": {
        "type": "string",
        "enum": ["endurance", "speed", "5k", "10k", "half_marathon"],
        "description": "Цель тренировки"
      },
      "days_per_week": {"type": "integer", "description": "Количество дней тренировок в неделю (3-6)"}
    },
    "required": ["fitness_level", "goal", "days_per_week"]
  }
}
```

### Инструмент 3: Рекомендация по питанию и восстановлению
```json
{
  "name": "get_recovery_recommendations",
  "description": "Возвращает рекомендации по питанию, отдыху и восстановлению для бегуна",
  "input_schema": {
    "type": "object",
    "properties": {
      "fitness_level": {
        "type": "string",
        "enum": ["beginner", "intermediate", "advanced"],
        "description": "Уровень физической подготовки"
      },
      "training_intensity": {
        "type": "string",
        "enum": ["light", "moderate", "high"],
        "description": "Интенсивность тренировок"
      }
    },
    "required": ["fitness_level", "training_intensity"]
  }
}
```

---

## 5. Структура проекта

```
anthropic-agent/
├── backend/
│   ├── build.gradle.kts                 # Конфигурация Gradle
│   ├── src/
│   │   ├── main/kotlin/
│   │   │   ├── Application.kt           # Точка входа, конфиг Ktor
│   │   │   ├── Agent.kt                 # Логика агента + Tool Use цикл
│   │   │   ├── Tools.kt                 # Реализация инструментов
│   │   │   ├── Models.kt                # Data classes для API
│   │   │   ├── AnthropicClient.kt       # HTTP клиент для API
│   │   │   └── Routes.kt                # API endpoints
│   │   └── resources/
│   │       └── application.conf         # Конфиг Ktor
│   └── .env                             # API ключ (не коммитить!)
│
└── frontend/
    ├── index.html                       # Веб-интерфейс
    ├── style.css                        # Tailwind стили
    └── script.js                        # JavaScript логика чата
```

---

## 6. Ключевые файлы Kotlin/Ktor

### 6.1 build.gradle.kts (зависимости)
```kotlin
plugins {
    kotlin("jvm") version "1.9.0"
    kotlin("plugin.serialization") version "1.9.0"
    id("io.ktor.plugin") version "2.3.0"
}

dependencies {
    implementation("io.ktor:ktor-server-core")
    implementation("io.ktor:ktor-server-netty")
    implementation("io.ktor:ktor-server-cors")
    implementation("io.ktor:ktor-client-core")
    implementation("io.ktor:ktor-client-cio")
    implementation("org.jetbrains.kotlinx:kotlinx-serialization-json:1.6.0")
    implementation("io.github.cdimascio:dotenv-kotlin:6.4.1")
}
```

### 6.2 Основные компоненты
- **Agent.kt** — Tool Use цикл, отправка запросов в Claude
- **Tools.kt** — реализация каждого инструмента
- **AnthropicClient.kt** — обёртка для API Anthropic
- **Routes.kt** — POST /chat endpoint

---

## 7. Frontend с Tailwind CSS

### Фичи интерфейса:
- 🎨 **Современный дизайн** — тёмная тема, градиенты, тени
- 💬 **Чат интерфейс** — сообщения расположены как в iMessage
- ⚡ **Плавные анимации** — загрузка, появление сообщений
- 📱 **Адаптивный** — работает на мобильных и десктопе
- 🚀 **Ноль фреймворков** — чистый JavaScript

### Структура HTML:
```
- Хедер с логотипом и инфо
- Контейнер для сообщений (скролл)
- Форма ввода внизу с кнопкой отправки
- Индикатор загрузки при обработке
```

---

## 8. Этапы реализации

### Фаза 1: Инициализация Backend
```bash
# Создать новый Gradle проект Kotlin
gradle init --type kotlin-application

# Добавить зависимости в build.gradle.kts
# Создать файлы: .env, Application.kt, Models.kt
```

### Фаза 2: Интеграция с Anthropic API
- Создать `AnthropicClient.kt` — клиент для API
- Реализовать функцию отправки запросов с tools
- Обработать ответы (включая tool_use блоки)

### Фаза 3: Tool Use система
- `Tools.kt` — реализация инструментов
- `Agent.kt` — логика цикла обработки инструментов
- Обработка ошибок и валидация параметров

### Фаза 4: API endpoints
- `Routes.kt` — POST /chat endpoint
- Управление историей сообщений в памяти
- CORS конфигурация

### Фаза 5: Frontend
- `index.html` с Tailwind CSS (через CDN)
- `script.js` с логикой отправки/получения
- Обновление UI в реальном времени

### Фаза 6: Тестирование и deploy
- Локальное тестирование
- Проверка Tool Use работы
- Оптимизация performance

---

## Пример работы запроса

### Пользователь спрашивает: "Помоги мне составить тренировочный план, я начинающий бегун, возраст 30, пробегаю 3км максимум"

```
1. Frontend отправляет POST на /chat:
   {"message": "Помоги мне составить тренировочный план, я начинающий бегун, возраст 30, пробегаю 3км максимум"}

2. Backend отправляет в Claude API с инструментами для бега

3. Claude анализирует и решает:
   - Нужно оценить уровень подготовки
   - Вызывает assess_fitness_level с параметрами

4. Backend обрабатывает:
   - assess_fitness_level(age=30, running_experience="beginner", weekly_runs=2, max_distance_km=3)
   - Возвращает: "Уровень: Beginner, рекомендуемые дни тренировки: 3-4"

5. Claude решает сгенерировать план:
   - Вызывает generate_training_plan(fitness_level="beginner", goal="5k", days_per_week=3)

6. Backend возвращает 4-недельный план:
   - Неделя 1: Легкие пробежки 2км, отдых, кросс-тренинг
   - Неделя 2: Интервальная тренировка, базовые пробежки
   - И т.д.

7. Claude получает рекомендации по восстановлению:
   - Вызывает get_recovery_recommendations(fitness_level="beginner", training_intensity="light")

8. Frontend получает финальный ответ с полным планом и рекомендациями
```

---

## 10. Рекомендации по реализации

### Backend (Kotlin):
```kotlin
// Используй data classes для типизации
data class Message(val role: String, val content: String)
data class ToolCall(val name: String, val input: Map<String, Any>)

// Сохраняй историю в простой List<Message>
val conversationHistory = mutableListOf<Message>()

// Обработка Tool Use в цикле
fun processToolCall(toolName: String, input: Map<String, Any>): String {
    return when(toolName) {
        "calculator" -> calculator(input)
        "get_current_time" -> getCurrentTime(input)
        "random_number" -> randomNumber(input)
        else -> "Unknown tool"
    }
}
```

### Frontend (Vanilla JS):
```javascript
// Простая отправка сообщения
async function sendMessage(text) {
    const response = await fetch('/chat', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ message: text })
    });
    
    const data = await response.json();
    displayMessage('user', text);
    displayMessage('assistant', data.response);
}

// Обновление UI - добавить сообщение в чат
function displayMessage(role, content) {
    const messageEl = document.createElement('div');
    messageEl.className = role === 'user' ? 'message user' : 'message assistant';
    messageEl.textContent = content;
    chatContainer.appendChild(messageEl);
}
```

### Tailwind CSS (через CDN):
```html
<!-- В <head> -->
<script src="https://cdn.tailwindcss.com"></script>

<!-- Потом просто используй классы -->
<div class="bg-gradient-to-r from-blue-500 to-purple-600">
  <p class="text-white text-lg font-bold">Агент готов!</p>
</div>
```

---

## 11. Быстрый чек-лист реализации

- [ ] Создан Kotlin/Ktor проект с зависимостями
- [ ] Файл `.env` с `ANTHROPIC_API_KEY`
- [ ] `AnthropicClient.kt` отправляет запросы в API
- [ ] `Tools.kt` реализует 3 инструмента
- [ ] `Agent.kt` обрабатывает tool_use блоки
- [ ] POST `/chat` endpoint работает
- [ ] Frontend index.html создан с Tailwind
- [ ] script.js отправляет сообщения и обновляет UI
- [ ] CORS включен в Ktor
- [ ] Сервер запущен на порту 8080
- [ ] Протестировано несколько вопросов

---

## 12. Запуск проекта

```bash
# Backend
cd backend
./gradlew run

# Frontend открыть в браузере
# http://localhost:8080

# Альтернативно - статические файлы через Ktor
# Поместить index.html, script.js в src/main/resources/static/
```

---

## 13. Ресурсы и документация

- **Ktor документация**: https://ktor.io/docs
- **Kotlin Coroutines**: https://kotlinlang.org/docs/coroutines-overview.html
- **Anthropic API Tool Use**: https://docs.claude.com/en/docs/build-with-claude/tool-use
- **Tailwind CSS**: https://tailwindcss.com/docs
- **HttpClient в Kotlin**: https://ktor.io/docs/client.html

---

## 15. Примеры диалогов для тестирования

```
Тест 1: Beginner
User: "Я только начал бегать, мне 25 лет, максимум могу пробежать 3км"
Claude: [вызывает assess_fitness_level]
        [вызывает generate_training_plan для beginner]
        [вызывает get_recovery_recommendations]
        Ответ: "Отлично! Я создал для тебя план..."

Тест 2: Intermediate  
User: "Я бегаю 2 года, хочу подготовиться к полумарафону, мне 35"
Claude: [assess_fitness_level → intermediate]
        [generate_training_plan → goal: half_marathon]
        Ответ: "Готовлю интенсивный план..."

Тест 3: Advanced
User: "Я опытный бегун, 40 лет, хочу улучшить скорость"
Claude: [assess_fitness_level → advanced]
        [generate_training_plan → goal: speed]
        Ответ: "Вот специальный план для улучшения скорости..."
```

---

## 14. Промпт для Claude Code (Kotlin версия)

```
Создай агента Anthropic на Kotlin/Ktor для составления тренировочных планов по бегу.

Специфика: Агент помогает создать персональный тренировочный план в зависимости от физической подготовки пользователя.

Backend (Kotlin/Ktor):
- Gradle проект с Ktor, kotlinx-serialization, HttpClient
- AnthropicClient.kt: HTTP клиент для Anthropic API (claude-haiku-4-5-20251001)
- Models.kt: data classes для Message, ToolCall, AnthropicRequest/Response
- Tools.kt: 3 инструмента:
  * assess_fitness_level(age, running_experience, weekly_runs, max_distance_km)
    Возвращает оценку уровня подготовки и рекомендации
  * generate_training_plan(fitness_level, goal, days_per_week)
    Возвращает 4-недельный план тренировок в подробном формате
  * get_recovery_recommendations(fitness_level, training_intensity)
    Возвращает рекомендации по питанию, сну и восстановлению
- Agent.kt: Tool Use цикл с обработкой tool_use блоков
- Routes.kt: POST /chat endpoint с управлением историей
- Application.kt: Ktor конфиг, CORS, запуск на порту 8080
- .env поддержка для ANTHROPIC_API_KEY
- System prompt для Claude должен быть ориентирован на тренировку по бегу

Frontend:
- index.html: Современный интерфейс специально для тренировочных планов
  * Тёмная тема с синими/оранжевыми градиентами (спортивный стиль)
  * Хедер с логотипом "Running Coach AI"
  * Чат интерфейс с красивыми сообщениями
  * Форма ввода с подсказками ("Я новичок...", "Хочу пробежать 5км..." и т.д.)
- script.js: Логика отправки сообщений, обновление чата, loading состояния

Инструменты должны возвращать:

1. assess_fitness_level:
   JSON с полями:
   - level: "beginner" | "intermediate" | "advanced"
   - max_weekly_km: максимум км в неделю
   - recommended_days: рекомендуемое кол-во дней тренировок
   - advice: краткие советы

2. generate_training_plan:
   JSON с полями:
   - week_1, week_2, week_3, week_4: подробное описание каждой недели
   - each week contains: Monday, Tuesday, Wednesday, Thursday, Friday, Saturday, Sunday
   - каждый день: тип тренировки, дистанция, интенсивность, время
   - tips: общие советы по плану

3. get_recovery_recommendations:
   JSON с полями:
   - nutrition: рекомендации по питанию
   - sleep: рекомендации по сну
   - stretching: упражнения на растяжку
   - injury_prevention: профилактика травм

Интерфейс должен быть красивым и удобным для взаимодействия с агентом.

System prompt для Claude:
"Ты - профессиональный тренер по бегу. Помогай пользователям создавать персональные тренировочные планы. 
Сначала оцени их физическую подготовку, затем предложи подходящий план.
Будь дружелюбен, мотивирующ и давай практические советы.
Используй доступные инструменты для анализа и создания планов."

Убедись что:
- Backend запускается на :8080
- Frontend доступен по http://localhost:8080
- Tool Use работает корректно (тестировать на разных уровнях подготовки)
- Чат ведёт естественный диалог с пользователем
- Нет hard-coded API keys в коде
- Frontend красивый и современный (Tailwind CSS)
```